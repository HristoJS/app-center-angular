import { NgDropFilesDirective } from './ng-drop-files.directive';

describe('NgDropFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDropFilesDirective(null);
    expect(directive).toBeTruthy();
  });
});
